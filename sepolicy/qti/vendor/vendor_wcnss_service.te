create_dir_file(vendor_wcnss_service, mnt_vendor_file)

# Allow cnss-daemon to access the property service
allow vendor_wcnss_service property_socket:sock_file write;

# Optionally allow use of specific vendor properties
set_prop(vendor_wcnss_service, vendor_default_prop)
